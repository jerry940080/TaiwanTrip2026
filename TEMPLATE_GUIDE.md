# 行程懶人包模板 使用說明

一套「單一 HTML 檔」的行程懶人包模板。改資料、不動引擎，就能做出新行程：
每日 SVG 路線地圖（捲動時畫線動畫）、時刻表、景點卡片（照片輪播＋介紹＋YouTube 內嵌）、
車票費用表與訂票時程。放上 GitHub Pages 即可分享，照片內嵌後離線也能看。

## 檔案

| 檔案 | 用途 |
|---|---|
| `index.html` | 網站本體。已換成台灣海岸線＋縣市界，資料區是待填骨架（含兩天示範行程） |
| `tools/embed_images.py` | 把照片轉 base64 塞進 HTML（`pip install pillow`） |
| `tools/make_land.py` | 重生地圖底圖（台灣／日本／全球海岸線） |
| `tools/make_poi.py` | 重生背景資訊層 `TOWNLINES`／`TOWNS`（鄉鎮區界與地名，可加村里層） |
| `tools/check.py` | 改完資料區跑一次，抓括號、key、座標的手滑 |
| `reference/example-atami-izu.html` | 完整範例（熱海・伊豆 8 天），要抄卡片或每日格式時打開它看 |
| `CLAUDE.md` | 用 Claude Code 協作時的專案慣例 |

## 製作新行程的順序

1. **標題與 hero**：改 `<title>`、hero 區的日期、標題、路線、交通概要三行。
2. **地圖底圖（第一段 script 的 `const LAND`）**：目前是全台灣（縣市界，eps 0.002）。
   只走某幾個縣市、想讓檔案更小或線條更細時重生：
   ```bash
   python3 tools/make_land.py --preset taiwan --regions 台北市 新北市 基隆市 宜蘭縣 --eps 0.0005 > land.js
   python3 tools/make_land.py --preset taiwan --all --eps 0.002 --min-pts 8 > land.js   # 全台（現在用的）

   # 只把某一小塊畫細（要放大到步行尺度時；現在 index.html 就多帶了八斗子這一塊）
   python3 tools/make_land.py --preset taiwan --all --eps 0.002 --min-pts 8 \
       --fine-eps 0.00015 --fine-lat0 25.125 --fine-lat1 25.155 \
       --fine-lng0 121.785 --fine-lng1 121.815 > land.js
   ```
   把輸出整段取代第一段 `<script>` 的內容。純市區行程不想要地形，也可以 `const LAND=[];`（地圖變純色底）。
   出國行程：`--preset japan --regions 静岡県 …`，或 `--preset world --lat0 … --lat1 … --lng0 … --lng1 …`。
3. **第二段 script 的資料區，由上而下改**：
   - `WIKI_LANG`：景點備援圖抓哪個語言的維基百科。台灣用 `'zh'`，日本行程改 `'ja'`。
   - `P`：所有會用到的地點 `{key:[緯度,經度]}`。key 用英文小寫。已預先放好台灣主要車站、機場與熱門景點。
   - `RAILS`：背景路網。每筆可帶 `c` 顏色、`w` 線寬、`d` 虛線樣式（`null` = 實線）、`o` 透明度、
     `maxSpan` 只在地圖緯度跨幅小於此值時才畫（捷運路網用 `.35`，避免全島圖被塞爆）。
     座標二選一：`pts`（P 的 key，會過平滑曲線）或 `ll`（原始 `[lat,lng]` 陣列，直接連折線，適合密集路網）。
     不需要可設 `const RAILS=[];`
   - `MODE`：交通方式的線色線型（高鐵／台鐵／捷運／客運／租車／渡輪／纜車／步行…），要新增照格式加一行。
   - `NAMES`：`{key:['顯示名','類型']}`，類型 `hotel`（朱紅＋🛏）/`spot`（粉點）/`station`（白圈）/`air`（黑菱形）。
   - `CARDS`：景點卡。格式：
     ```js
     key:{name:'中文名',jp:'原文／英文名',kind:'景點',short:'卡片上的一句話',
          wiki:['維基百科條目名'],            // 沒自備照片時的備援圖（連網才會載）
          video:[{u:'https://youtu.be/xxxx',l:'標籤可空'}],  // 可省略；網址帶 t=53s 會從該秒播
          text:'展開後的完整介紹（2–4 句）',
          chips:['開放時間','費用','交通']},
     ```
   - `IMG`：留 `{}`，照片之後用工具塞（見下）。
   - `DAYS`：每天一個物件：
     ```js
     {n:1,date:'2/10',wd:'三',title:'A → B',sub:'副標一句',stay:'今晚飯店',
      map:{pts:['要顯示的點'],legs:[{a:'起',b:'訖',mode:'tra',via:['中途點'],curve:.2}],
           labels:{key:'t|b|l|r'},minSpan:.1},   // minSpan=地圖最小緯度跨幅，市區日 .02、跨縣市日 .2–.3
      tl:[{t:'09:00',i:'🚄',h:'標題（只放標題，不放長描述）',move:1}],  // move:1 淡藍底=移動列
      cards:['cardKey'],
      rain:'<b>雨備</b>　...'}                    // 可省略
     ```
   - 首頁總覽圖：搜 `const cfg={pts:` 那段（在 RENDER 區開頭），放整趟的大路線。
4. **HTML 區塊**：行前一頁看懂（住宿表）、車票與費用、訂票時程、美食與行李，直接改表格文字。
5. **照片**：
   ```bash
   # 照片多的時候用外部檔案（現在的做法）：IMG 存相對路徑，頁面才不會肥
   python3 tools/embed_images.py index.html --out assets/img jiufen=1.jpg,2.jpg
   python3 tools/embed_images.py index.html --out assets/img +jiufen=3.jpg   # 追加

   # 只有幾張、想維持單檔可攜時才用 base64 內嵌（省略 --out）
   python3 tools/embed_images.py index.html jiufen=1.jpg,2.jpg   # 覆蓋，第一張=封面

   # 分組：卡片裡再切分頁，用 | 分組、組名和檔案之間用 :
   python3 tools/embed_images.py index.html --out assets/img \
       "beitou=瀧乃湯:a.jpg,b.jpg|春天酒店大眾池:c.jpg"
   ```
   截圖先裁掉浮水印／Google 鏡頭圖示（常在底部 8–9%）。工具會自動縮 1200 寬、轉 JPEG q82。
6. **檢查**：`python3 tools/check.py`，沒問題再開瀏覽器看一次。
7. **部署**：push 後到 GitHub → Settings → Pages → Source 選分支與 `/ (root)`，網址即 `https://<帳號>.github.io/TaiwanTrip2026/`。
   注意：YouTube 內嵌需要 referrer，**本機 file:// 開啟影片會顯示錯誤 153**，上了網址就正常。

## 每天的提醒事項：固定五欄

DAYS 的每一天用 `notes` 寫提醒，**只有這五個欄位**，沒寫的不會顯示：

```js
notes:{
  pack:'要帶什麼——只寫這天特別需要的（外套、防滑鞋、雨具）',
  tickets:'有哪些票要訂／現場怎麼買、多少錢',
  book:'有哪些東西可以預訂（餐廳、接駁、湯屋、導覽車）',
  other:'其他。條列多的時候直接寫 <ul><li>…</li></ul>',
  rain:'雨備'
}
```

寫的時候只留「讀者要做的事」，不要寫「我為什麼這樣排」——後者是聊天內容，不是行程頁的內容。
一天控制在 500 字以內；超過就代表有東西該搬去卡片或砍掉。

舊的 `rain:'一整段文字'` 仍然支援（`reference/` 的範例還在用），但新的日子請一律用 `notes`。

### 卡片的官網連結

卡片加 `url` 欄位，點開卡片時**標題就會變成連到官網的超連結**（另開分頁，右上角有 ↗）。
沒寫 `url` 的卡片標題維持純文字。

```js
huashan:{name:'華山1914文化創意產業園區',jp:'Huashan 1914',kind:'景點',
  url:'https://www.huashan1914.com/w/huashan1914/index', …}
```

網址裡的 `&` 要寫成 `&amp;`（值會進 `innerHTML`），例如
`url:'https://www.taiwan.net.tw/m1.aspx?sNo=0001016&amp;id=A12-00359'`。

連結放標題、不放卡片格的理由：卡片格本身是 `role="button"`、點了開卡片，
再塞一個連結進去會有兩個點擊目標。`text` 裡仍可另外放連結，用在
「線上訂票」「查當期檔期」這種有動作的地方。

### 卡片裡的照片分頁

一張卡片的照片如果本來就分成幾類（例如「北投溫泉」底下有五家不同的湯屋），
用分組把它們切成分頁，模態框會在照片下方長出一排按鈕，點了跳到該組第一張，
左右翻頁時按鈕也會跟著亮。組名就是分頁的標題。

`IMG` 的值因此有三種形狀，引擎都吃：

```js
IMG={
  cks:['assets/img/cks-1.jpg','assets/img/cks-2.jpg'],            // 一般相簿
  beitou:[{t:'瀧乃湯',u:['assets/img/beitou-2.jpg', …]}, …],        // 分組，t 是分頁標題
  foo:'assets/img/foo-1.jpg'                                       // 只有一張
}
```

只有一組（或沒有組名）時分頁列不會出現。**不要手動改 `IMG`**，用工具的分組語法產生。

## 地圖的背景資訊層

底圖除了海岸線，還有三層會依地圖遠近自動開關（在引擎的 `areaBg`／`stationBg`／`railBg` 裡）：

| 圖層 | 何時出現 | 常數 |
|---|---|---|
| 縣市名 | 任何跨幅都畫（依畫面範圍裁掉看不到的） | — |
| 區界虛線＋區名 | 跨幅 ≤ `.35`（台北尺度） | `TOWN_SPAN` |
| 捷運路網彩線 | 跨幅 ≤ `.35` | 各線的 `maxSpan` |
| 捷運站圓點 | 跨幅 ≤ `.115` | `STATION_SPAN` |
| 捷運站名 | 同上，且畫面內站數 ≤ 48 | `STATION_LABEL_MAX` |

判斷「跨幅」用的是 `cfg.detail`（沒設就用地圖自己的 `bbox`）。
`cfg.detail` 讓「整體範圍」和「畫多細」分開：首頁大圖只框台北一帶，但鏡頭會平移到南投，
就靠 `bbox` 決定基準比例、`detail` 決定背景細到哪一層。
縣市名的範圍會自動涵蓋各 leg `zoom` 會平移到的地方，否則鏡頭移過去只剩一片空白。

### 同一天放兩張地圖

景點全擠在幾百公尺內的日子（10/3 的八斗子潮境就是），跟全日路線畫在同一張圖上一定糊掉。
這時在 DAYS 那天加 `map2`，就會在時刻表左邊上下疊兩張圖：

```js
map:{cap:'全日路線：南京三民 → 瑞芳 → 深澳線', pts:[...], legs:[...], minSpan:.12},
map2:{cap:'步行段：八斗子車站 → 潮境 → 海科館站', pts:[...], legs:[...],
      bbox:{la0:25.1345,la1:25.1465,lo0:121.7915,lo1:121.8095}},
```

`cap` 是圖上方的小標題，兩張都要寫，不然讀者分不出哪張是哪張。放大的那張建議直接給
`bbox`（不要用 `minSpan` 自動算），比較好控制留白。類名用的是 `.maps.stack`——
**不要改成 `.two`**，樣式表上面已經有一個 `.two` 是 `1fr 1fr` 的兩欄 utility，會把兩張圖排成左右並列。

放大到幾公里的尺度時，底圖也要跟著補：

- **海邊**：把 `LAND` 那一區重生成細的（見上面 `--fine-eps`），不然海岸線會粗到看起來像景點掉在海裡。
- **內陸**：跑 `tools/make_poi.py` 補該區的鄉鎮界，幾公里的尺度還要加 `--village-towns`
  補村里層——鄉鎮界在那個尺度下整個鄉只有一個地名點，放大後會落在畫面外，圖上會一片空白。
- **內陸湖泊與河流目前沒有圖層**。`LAND` 是縣市界（只有海岸線），所以日月潭那張圖畫不出湖面。

每張地圖右下角有「簡化」按鈕，按了會隱藏這些背景細節（`.mapcard.plain .poi{display:none}`），
只留路線與景點。覺得某天的地圖太擠，先調 `minSpan` 或直接讓讀者自己按簡化。

## 地圖動畫：跟著行程平移

`legs` 的順序就是動畫繪製的順序，所以把路線按日期排好，捲進畫面時就會照時間長出來。
每一段還可以帶兩個欄位控制鏡頭：

| 欄位 | 用途 |
|---|---|
| `zoom:{la0,la1,lo0,lo1}` | 播到這段時，把畫面平移／放大到這個經緯度範圍 |
| `zoom:'reset'` | 播到這段時拉回原本的全圖 |
| `pause:1600` | 這段畫完後多停留幾毫秒再往下（配合 zoom，讓鏡頭在當地多留一會） |
| `day:'9/27'` | 這段屬於哪一天，用來做「只留最近兩天」的滾動視窗（見下） |

另外 `cfg.padLeft`（0–1）會在放大時於左側預留一段空白——放大後的地形會被推到右側，
用在左邊壓著文字的地圖。首頁大圖改成直接把 `bbox` 的經度中心往西挪來留白，就不用這個了。

範例（首頁總覽圖在南下時鏡頭跟著移到南投，北返時拉回原本的台北視角）：

```js
{a:'home',b:'taichungHsr',mode:'hsr',zoom:{la0:23.60,la1:24.17,lo0:120.45,lo1:120.76}},
{a:'xitou',b:'taichungHsr',mode:'bus',pause:1600},
{a:'taichungHsr',b:'home',mode:'hsr',zoom:'reset'},
```

`zoom` 的框會先補成畫布長寬比（1200×440，很扁），所以像南投這種南北向的區域，
框給得再緊，補完還是會左右各留一大片。想讓景點落在畫面右側，就把框的**經度中心**往西挪。

### 滾動視窗：一天過完就淡掉

路線一多，全部疊在台北市區會糊成一團。給每個 leg 加 `day`，動畫就會只留最近兩天：

- **當天**：原本的顏色
- **前一天**：轉灰（`#b1aa9c`，透明度 .65）
- **再更早**：淡出消失

`day` 的值就是天數的標籤字串，引擎用**出現順序**排名，所以 `'9/30–10/2'` 這種合併的
區塊算成一天。沒寫 `day` 的 leg 完全不受影響（每日地圖就是這樣，維持全部同時顯示）。

動畫播完會全部亮回來，停在完整的總覽狀態——滾動視窗是給「看行程怎麼走」用的，
停下來時還是要能一眼看完整趟。

尊重系統的「減少動態」設定：開啟時直接顯示完整路線，不做平移，也不做滾動視窗。

## 引擎行為備忘

- 地圖 bbox 由 `pts` 自動計算，`labels` 控制標籤在點的上/下/左/右，重疊就換方位或加大 `minSpan`。
- `cfg.names` 可以只在這張圖上換掉某個點的顯示名（首頁大圖點很密，就靠它把
  「家（西松高中）」縮成「家」、「故宮博物院」縮成「故宮」）；`cfg.kinds` 同理換樣式。
- 同一段路來回會疊線，回程 leg 加 `curve:0.2`（負值彎另一邊）。
- 卡片點開＝彈窗：上照片輪播（←→、圓點、手機滑動）、中文字、分隔線下影片。
- 每日地圖捲入視窗會播路線動畫，`重播路線` 鈕可重看；尊重系統「減少動態」設定。
- 桌機把滑鼠移到首頁大地圖會展開完整比例。
- 檔案大小：引擎＋台灣底圖約 130 KB，每張照片約 100–250 KB，20–30 張照片的成品約 3–7 MB。

## 風格慣例（想維持同一系列感的話）

和紙底 `#f8f5ee`、藍 `#2b4c7e`、櫻粉 `#e28aa5`、朱紅 `#c8452e`（住宿／租車）；
標題 Noto Serif TC。時刻表只放標題；估算時間寫「約」；文案繁體中文、專名保留原文。

## 真實地圖底圖（Leaflet）

每張日圖現在有兩層：原本的手繪 SVG，加上疊在上面的真實地圖（Leaflet）。
兩層共用同一份 `map` / `map2` 設定，路線、顏色、線型都一致，不必為了真實地圖
另外寫資料。

圖磚來源寫在 `TILES` 陣列裡，依序試，前一個 4 秒內生不出圖磚就換下一個：

1. **內政部國土測繪中心 臺灣通用電子地圖**（`wmts.nlsc.gov.tw/wmts/EMAP/...`）
   ——官方圖資、中文地名、免申請金鑰，台灣的行程用這個最合適。
   注意網址是 WMTS 的 `{z}/{y}/{x}`（TileRow 在前），不是一般的 `{z}/{x}/{y}`。
2. **OpenStreetMap 官方圖磚**——保底用。

**不要用 CARTO 的免費圖磚**：`basemaps.cartocdn.com` 已經改成要 API key，
沒有金鑰時它照樣回傳圖片（所以 `tileload` 會過），但整張圖蓋滿
「API KEY REQUIRED」浮水印。換圖磚來源時要親眼看過長什麼樣，不能只看有沒有載成功。

**載不到就自動退回手繪圖。** Leaflet 的 CDN 被擋、離線、圖磚伺服器掛掉，
都只會讓真實地圖那層不出現，畫面維持原本的手繪 SVG，不會開天窗。判斷的依據是
`tileload`（真的有一張圖磚畫出來）而不是 Leaflet 的 `load`——後者在圖磚全部
載失敗時也會觸發。等 8 秒沒等到就把那層拆掉。

圖卡右下角的「手繪路線圖／真實地圖」按鈕可以隨時切換；只有真實地圖成功載入時
才會出現。切到真實地圖時「簡化」「重播路線」會收起來，那兩個是手繪圖的功能。

改動時要注意的兩個坑（都踩過）：

- **CSS 選擇器要指名到 `.mapwrap` 的直接子元素。** `.mapcard svg{width:100%}`
  這種寫法會連 Leaflet 畫線用的 SVG 一起打到，線條會被撐成一整塊色塊；
  `.mapcard.real .mapwrap svg{visibility:hidden}` 則會把 Leaflet 的線一起藏掉。
- **手繪 SVG 要自己 `position:relative;z-index:1`。** 它原本是非定位的行內元素，
  繪製順序排在 `position:absolute` 的 `.rmap` 之前，圖磚載不到時會被 `.rmap`
  的底色整片蓋掉，畫面變成一張空白的圖——而且 `visibility` 量起來還是 `visible`，
  只看 computed style 抓不到。改這幾行之後要<b>實際截圖看</b>，或量截圖的顏色數。
- **`curve` 要換算回經緯度。** 手繪版是在螢幕座標上彎（`legPath`），真實地圖得用
  `legLatLngs()` 換算，乘上 `cos(lat)` 補經度被壓縮的比例，兩張圖才會彎向同一邊。

## 周邊地標層 POIS

`tools/make_pois.py` 從國土測繪中心地標開放資料撈出路線附近的景點、公園、車站，
輸出 `const POIS=[...]`，放大到 `POI_SPAN` 以內的尺度時畫成淡色小點＋地名。
手繪圖沒有水域與道路圖資，光靠行程那幾個點會很空，這一層就是拿來補的。
真實地圖本身就有這些資訊，所以只有手繪那層會畫。

### `who`（同行者）

DAYS 每一天可以加 `who:'...'`，會在日期標題右邊多一個藍色左邊條的方塊，
排在「今晚住」前面。沒寫就不顯示。

允許放 HTML，備註用 `<span class="note">…</span>` 包起來會變小字灰色：

```js
who:'吳老媽・吳老爸・Nick <span class="note">三位裡面來兩位</span>',
```

行前區另有一張「同行者」總表，是手寫的 HTML，改了 `who` 記得一起改。

### 捷運路線怎麼畫（`via` ＋ `sharp`）

兩站之間拉一條直線在跨縣市的圖上看不出問題，但在台北市區的圖上會明顯不像實際
走法。要讓線沿著捷運路網走，把中間停靠的站當 `via` 一路列出來：

```js
{a:'xiaojudan',b:'longshan',mode:'metro',
 via:['njfx','sjnj','mZhongshan','mBeimen','ximen'],sharp:1},
```

- **`via` 的站點座標**取自本檔 `STATIONS`（`m` 開頭那幾個 key 就是專門拿來當轉折點的，
  例如 `mZhongshan`、`mXiaonanmen`）。只當 via 用，不會畫成行程點。
  要新的站就從 `STATIONS` 抄座標進 `P`，再給 `NAMES` 一個顯示名。
- **`sharp:1`＝直線接每個 via，不要平滑曲線。** 捷運這種沿著站點折過去的路線，
  用預設的 Catmull-Rom 曲線畫會在轉折處外擴（像龍山寺→西門→小南門這種折返會鼓出來），
  直線才像路網。真實地圖那層本來就是直線，加了 `sharp` 兩張圖才一致。

**不要**改用 `METRO` 的折線去吸附：那份資料是簡化過的，**信義線在大安森林公園以東
是缺的**，吸附會畫出斷掉的線。`STATIONS` 才是完整的。


---

## 大字版 `simple.html`

給長輩在手機上看的簡化頁，跟 `index.html` 沒有共用程式，改一邊不會影響另一邊。

### 資料長什麼樣

```js
{n:4,date:'9/29',wd:'二',t:'九份泡茶看夕陽',s:'十點才出門，全程有位子坐',
 stay:'茹曦酒店（台北）',rest:0,
 steps:[['10:00','捷運站集合，今天晚一點出門'], …],
 tip:'今天的重點就是<b>坐在茶樓裡看海等天黑</b>，不用一直走。'}
```

| 欄位 | 用途 |
|---|---|
| `n` `date` `wd` | 第幾天、日期、星期——**必須跟 `index.html` 的 `DAYS` 一模一樣**，`check.py` 會比對 |
| `t` | 一句話標題，列表與內頁都用它 |
| `s` | 列表上那行小字，寫「今天輕鬆還是累」 |
| `stay` | 今晚住哪。可以比 `DAYS.stay` 多寫地區（「茹曦酒店（台北）」），`check.py` 只檢查有沒有包含 |
| `rest` | `1` 的話列表那格變綠框（休息日） |
| `steps` | `[時間, 一句話]`，**最多六條**。時間可以是「整天」「下午」這種非數字 |
| `tip` | 選填，黃底那一塊。放「可以不去」「要帶外套」這類提醒 |

`CONTACTS` 是底部的卡片：`lab`（小字說明）、`nm`（名稱）、`ad`（地址）、`tel`／`telLab`（選填，撥號按鈕）。
`tel` 只寫數字，不要空格或括號。

### 寫的時候注意

- **一天最多六句。** 超過就要砍，不是縮小字。
- **講要做什麼，不講為什麼。** 「搭客運直達九份，約 1 小時 20 分」就夠了，不用寫路線號碼、不用寫為什麼選這條。
- **走不動的地方要寫出口。** 有階梯、要走遠的，在 `tip` 裡明講可以不去或只看一半。
- **不要放地圖和照片**——載入慢，而且在大字級下會把版面擠爆。
- **字級**由 `--base` 控制（21／25／30px），存在 `localStorage` 的 `bigfont`。
  所有尺寸都用 `rem`，所以改 `--base` 整頁跟著放大。新增樣式時不要寫死 `px` 字級。
- **按鈕最小 60px 高**，長輩手指點得到。
- 內頁用網址的 `#d4` 切換，手機的返回鍵才會回到列表——不要改成純 JS 的 show/hide。

### 改完要檢查

`python3 tools/check.py` 會比對兩個檔案的日期、星期、住宿。它**不會**檢查 `steps` 的內容，
所以行程時間有動的時候，記得自己對一次。


---

## 紙本版 `台灣行程.docx`

給長輩列印當筆記用。**不是重新排版的文字，是把 `index.html` 的畫面切成一頁一張貼進 Word**——
版面、照片、地圖、顏色都跟螢幕上看到的一樣。

```bash
NODE_PATH=<playwright 位置> node tools/shoot_pages.js   # 整頁截圖 + 算切點
python3 tools/make_docx.py                              # 切圖並打包成 docx
```

`assets/print/` 是產生出來的東西，沒有進版控，每次都要重跑這兩步。

### 為什麼渲染寬度是 680px

**渲染寬度決定印出來的字多大。** 圖是等比例縮到 A4 的 19cm 寬，所以渲染得愈窄，
同一個字在紙上就愈大：

| 渲染寬度 | 內文 15px 印出來 |
|---|---|
| 1240px | 6.5pt ← 長輩看不到 |
| 900px | 9.0pt |
| **680px** | **11.9pt** ← 現在用這個 |
| 620px | 13.0pt |

680px 還有一個好處：在 860px 斷點以下，`.pre`、`.day-body`、`.maps` 都會變成單欄，
本來就比較適合紙本。再窄就會掉進 640px 的手機版面，反而鬆散。

### 切頁怎麼切

不是固定高度切下去，不然標題、照片、表格的一列都會被剖成兩半。`shoot_pages.js` 做兩件事：

1. **收集不可以切開的東西**（`.day-head`、`.mapcard`、`.ph`、`tr`、`li`、`p`、`h1`–`h3`…），
   任何落在它們內部的切點直接淘汰。
2. **優先切在「新的一天開始」的位置**，讓每天從新的一頁開頭。但如果這樣會讓前一頁
   填不到 55%，就退回一般切點——不然會印出大半張空白的紙。

目前 30 頁，平均填滿約 91%。

### 圖片格式是逐頁挑的

每頁 PNG 與 JPEG 各存一次、留比較小的那個。整頁都是字的頁面顏色少，**PNG 反而比 JPEG 小**；
有照片的頁面則是 JPEG 小很多。兩種印出來都看不出差別。現在 30 頁裡 15 頁 PNG、15 頁 JPEG，
總共約 11 MB。

### 截圖前要先處理的事

`shoot_pages.js` 裡有一段是把「只有在螢幕上才有意義」的東西藏掉或定格，改版面時要跟著維護：

- 置頂導覽列 `nav.strip`——不藏的話每一片都會重複出現
- `.poitog`、`.replay`、`.bigver`、頁尾的 Word 下載連結
- 地圖的路線動畫要直接跳到最後一格（`strokeDasharray`、`opacity`）
- **照片是 lazy load 的**，要先整頁捲過一遍並等 `onload`，不然截到的是空白
- 載不到的照片（維基百科那幾張）整格藏掉，免得留一個灰框

### 沙箱裡看不到的東西

這台機器連不到圖磚伺服器與維基百科，所以截出來的是：

- **手繪地圖**，不是疊在上面的真實地圖圖磚（`REAL MAP` 那層退回手繪版）
- **24 張照片裡有 3 張是空的**（那幾張是即時向維基百科要的），已經整格藏掉

在看得到網路的機器上重跑，這兩個就會是真的內容。

### 怎麼檢查

用 LibreOffice 轉 PDF 再逐頁看：

```bash
soffice -env:UserInstallation=file:///tmp/lo --headless --norestore \
        --convert-to pdf --outdir /tmp/pdf 台灣行程.docx
```

`libreoffice-core` 單獨裝是**打不開 .docx 的**（會報 source file could not be loaded，
跟檔案本身無關），要有 `libreoffice-writer`。
